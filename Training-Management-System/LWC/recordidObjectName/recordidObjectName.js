import { LightningElement,api } from 'lwc';

export default class RecordidObjectName extends LightningElement {
   @api recordId //it is for any record what we view at that movement
  @api  objectApiName //at object what we view at that movement

}